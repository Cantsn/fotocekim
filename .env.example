# SQLite (local)
DATABASE_URL="file:./dev.db"

# Docker: file:/data/fotocekim.db

NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_SITE_NAME=FotoCekim
NEXT_PUBLIC_WHATSAPP=905000000000

ADMIN_EMAIL=admin@fotocekim.com
ADMIN_PASSWORD=admin123

# Google Calendar OAuth (Admin → Takvim)
# Google Cloud Console → APIs → Google Calendar API enable
# OAuth Client (Web) redirect URI: {NEXT_PUBLIC_SITE_URL}/api/google/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Optional override:
# GOOGLE_REDIRECT_URI=https://nextcan.net/api/google/callback
